In the debug info, sort the matched cards by similarity (now sometimes a low similarily card shows first.)
Refactor the GUI:
The "live preview" should contain the same information - the live video feed with card matching bounding boxes. However, the "Last scan result" part of the gui could instead  